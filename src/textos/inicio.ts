/**
 * Textos de tela ficam aqui, nunca soltos no JSX (plataforma/CLAUDE.md). O
 * verificador de texto (npm run checar-texto) le este arquivo tambem.
 */
export const textosInicio = {
  descricao: "Esta e a base do painel. As telas de cada etapa entram por aqui, uma de cada vez.",
};
